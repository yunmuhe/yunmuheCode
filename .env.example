# 前端环境变量配置示例
# 复制此文件为 .env 并修改配置

# API服务器地址（可选，默认根据平台自动选择）
# VITE_API_BASE_URL=http://127.0.0.1:5000
# UNI_APP_API_BASE_URL=http://127.0.0.1:5000

# 是否启用自动服务器发现（默认：true）
# 设置为 false 或 0 可禁用自动发现，直接使用配置的URL
# VITE_AUTO_DISCOVER=false
